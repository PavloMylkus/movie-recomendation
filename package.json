{
	"name": "pet-app",
	"version": "1.0.0",
	"description": "Movies recomendation demo app: React + GraphQL.",
	"main": "./server/src/index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node server/src/index.js",
		"build": "cd client && npm run build",
		"install": "cd client && npm install --legacy-peer-deps && cd ../server && npm install"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/PavloMylkus/movie-recomendation.git"
	},
	"author": "Mylkus Pavlo",
	"license": "ISC",
	"engines": {
		"node": "16.x"
	}
}